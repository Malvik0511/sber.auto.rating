import styled from "styled-components";
import { Select } from "@sberdevices/plasma-web";

export const SelectStyled = styled(Select)`
    button {
        height: 56px;
        background: var(--plasma-colors-surface-liquid01);
        border-radius: 16px;
    }
`
